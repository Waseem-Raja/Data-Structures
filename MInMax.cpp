#include <iostream>
using namespace std;

/*int main()
{
    int arr[6] = {2, 3, 43, 3, 2, 6};

    for (int i = 0; i < 6; i++)
    {
        if (arr[0] > arr[i]) //min element
            arr[0] = arr[i];
    }
    cout << "Minimum element is " << arr[0] << endl;
    for (int i = 0; i < 6; i++)
    {
        if (arr[0] < arr[i]) //max element
            arr[0] = arr[i];
    }
    cout << "Maximum element is " << arr[0];
}*/
int main()
{
  int arr[]={2,3,4};
  cout<<*(arr+1);



}